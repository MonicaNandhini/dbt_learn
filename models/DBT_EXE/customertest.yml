version: 2

models:
  - name: cust_table
    columns:
    - name: o_orderkey
    - name: o_custkey
      tests:
      - relationships:
          to: source('DBT_EXE','CUSTOMER')
          field: c_custkey
